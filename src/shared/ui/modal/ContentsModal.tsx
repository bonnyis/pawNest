import {} from "react";

type ContentsModalProps = {
  width?: number;
  height?: number;
  className?: string;
  onClose: () => void;
};

const ContentsModal = ({}: ContentsModalProps) => {
  return <div>ContentsModal</div>;
};

export default ContentsModal;
